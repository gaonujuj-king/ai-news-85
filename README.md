# AI 인사이트 데일리

공개 Google News RSS를 이용하는 모바일 뉴스 브리핑 앱입니다. 뉴스 수집과 표시에 유료 AI API나 API 키를 사용하지 않습니다. 요약은 RSS 설명문을 줄여 표시합니다.

## 기준 배포와 프로젝트 구조

운영 기준은 **Vercel + 공유 비밀번호 로그인**입니다. 앱 화면과 정적 리소스의 유일한 원본은 `docs/`입니다. 루트와 `public/`에 있던 이전 앱 사본은 제거했습니다.

- `docs/`: 앱 화면, 아이콘, PWA 설정, 저장된 뉴스 JSON
- `lib/briefing.js`: 공통 RSS 수집·분류·중복 제거·정렬
- `lib/video-feeds.js`: 안될공학·Stanford HAI·Google DeepMind·EBS 공식 YouTube Atom 피드에서 최근 30일의 AI 연구·해설 영상 선별
- `lib/published-briefing.js`: GitHub에서 예약 수집 결과 조회, 5분 캐시, 실패 시 이전 데이터 제공
- `lib/auth.js`: 공유 비밀번호와 세션 쿠키 검증
- `api/app.js`: 인증 후 허용된 앱 파일과 뉴스 제공
- `api/login.js`, `api/logout.js`: 로그인·로그아웃
- `scripts/refresh-news.mjs`: 공통 수집기로 `docs/data/latest.json` 생성
- `.github/workflows/daily-news.yml`: 한국 시간 오전 7:30 예정 수집 작업
- `tests/app.test.js`: 인증과 공통 수집기 회귀 검사

`api/`에는 HTTP 요청을 처리하는 함수만 둡니다. 공통 모듈은 `lib/`에서 관리합니다. 삭제된 `server.mjs`와 이전 `/api/recommendations` 방식은 사용하지 않습니다.

## 매일 자동 갱신

`Google News RSS → GitHub Actions → main의 docs/data/latest.json → Vercel 인증 → 앱`

- GitHub Actions가 한국 시간 오전 7:30 예정으로 실행되어 JSON을 갱신합니다. 수동 실행과 수집 코드 변경 시에도 실행됩니다. GitHub 실행 상황에 따라 지연될 수 있습니다.
- Vercel은 로그인 후 GitHub의 최신 JSON을 읽습니다. 뉴스만 바뀔 때는 Vercel 재배포가 필요하지 않습니다. 함수의 5분 캐시와 GitHub 배포 캐시만큼 반영이 늦어질 수 있습니다.
- GitHub 조회 실패 시 함수가 보유한 마지막 정상 데이터 또는 배포에 포함된 JSON을 표시합니다. 앱에 마지막 갱신 시각과 이전 데이터 사용 여부를 표시합니다.
- 마지막 수집으로부터 36시간이 지나면 갱신 지연으로 표시합니다. 실패한 작업은 GitHub Actions에서 확인하고 다시 실행할 수 있습니다.

추가 AI API 키, 데이터베이스, Vercel 배포용 예약 토큰은 필요하지 않습니다. 공개 저장소를 사용하므로 뉴스 JSON 자체는 공개 자료입니다. 저장소를 비공개로 전환하면 현재 조회 방식도 변경해야 합니다.

영상은 선별 기사 최대 18개와 별도로 채널당 최대 2개, 총 6개를 한국어 우선, 같은 언어 내 업로드 날짜순으로 제공합니다. 영어 영상은 별도로 표시합니다. 홈에는 3개, 영상 탭에는 전체 수집 결과를 표시합니다. 영상 수집이 실패하거나 관련 영상이 없으면 상태 안내를 표시하며 예시 영상을 보여 주지 않습니다. 채널 목록은 `lib/video-feeds.js`에서 관리합니다. 새 업로드는 다음 예약 수집 때 반영됩니다.

## Vercel 설정

- 저장소: `gaonujuj-king/ai-news-85`
- 앱 주소: https://ai-news-85.vercel.app/
- Root Directory: 저장소 루트
- Framework Preset: Other (`vercel.json`의 `framework: null`)
- Output Directory: `.` (`vercel.json`에서 명시)
- 환경 변수: `APP_PASSWORD`를 Production에 설정하고 재배포

모든 앱 파일 요청은 `api/app.js`의 인증 검사를 통과해야 합니다. `docs`나 `public`을 별도 정적 배포 폴더로 지정하지 마세요. 비밀번호와 `.env`는 커밋하지 않습니다. 비밀번호 입력은 HTTPS 환경에서 사용합니다.

비밀번호 보호는 Vercel 앱 접근을 제한합니다. 공개 GitHub 저장소의 코드와 뉴스 JSON까지 비공개로 바꾸지는 않습니다. 기존 GitHub Pages 공개 배포는 2026-09-20에 비활성화했습니다.

## 로컬 검증

Node.js 22 이상에서 실행합니다. Vercel과 Actions는 Node.js 22를 기준으로 합니다.

```powershell
npm test
```

검사는 테스트용 비밀번호와 가상 RSS를 사용하며 실제 비밀번호나 외부 네트워크가 필요하지 않습니다.

```powershell
npm run refresh
```

이 명령은 인터넷을 사용해 `docs/data/latest.json`을 변경합니다. 루트 `index.html`이나 `public/` 대신 `docs/index.html`을 수정하세요. 앱 전체 확인은 Vercel의 인증 경로를 포함해야 합니다.

## 2026-09-20 배포 점검

- 변경 전: Vercel이 로그인 없이 열렸고 9월 19일 데이터가 남아 있었습니다.
- 변경 후: 커밋 `524345f`의 Vercel 배포 성공을 확인했습니다. 루트, 뉴스 JSON, 직접 파일 경로가 모두 비로그인 상태에서 로그인 화면으로 연결됩니다.
- [GitHub 자동 수집 실행](https://github.com/gaonujuj-king/ai-news-85/actions/runs/35487286658)이 성공해 `2026-09-20T03:42:59.575Z` 기준 기사 18개를 저장했습니다.
- 운영용 데이터 조회 모듈에서 실제 GitHub JSON을 읽어 `source: github-actions`, `stale: false`, `fallback: false`를 확인했습니다.
- 기존 Pages 배포 설정을 해제하고 Pages API의 404 응답을 확인했습니다.
- 로컬 5개 검사에서 인증 흐름, 다음 날 데이터 반영, 캐시 만료, 통신 실패 시 이전 데이터 표시와 복구를 검증했습니다. 실제 사용자 비밀번호로 로그인한 후 화면 확인은 사용자 계정에서 진행할 수 있습니다.

## 아직 남은 기능

검색·주제 필터·보관함 영구 저장·자동 푸시/메일 발송은 후속 단계입니다. 현재는 앱을 열어 브리핑을 확인하는 방식이며, 알림 스위치는 예약 발송 기능을 의미하지 않습니다.

## 추천 선별 기준

- 언론사 이름만 믿지 않고 RSS의 원출처 도메인을 확인합니다. EBS·KBS·MBC·SBS·연합뉴스·BBC·Reuters·한겨레·경향신문·한국일보·JTBC·사이언스타임즈를 초기 수집 대상으로 사용합니다. 포털 재전송처럼 원언론사를 확인하기 어려운 링크는 제외합니다.
- 최근 7일 기사 중 제목에 분석·해설·연구·정책 쟁점 등이 드러나는 내용을 우선합니다. 기존 AI 주제에 교육 정책과 노동·기후·복지 해설을 포함합니다.
- 행사 개최·업무협약·수상·신제품·할인·모집 등 단순 홍보성 제목과 과장·선정적 제목을 제외합니다. 행사 자체를 비판하거나 검증하는 분석은 허용합니다. 연구 결과 발표 자체를 일괄 제외하지 않습니다.
- 제목·URL 중복을 제거하고 언론사당 최대 3개, 주제당 최대 4개로 제한합니다. 의견·칼럼은 별도 표기합니다.
- 영상은 전문 기술 채널·대학·연구기관·교육 방송 중 AI 주제와 연구·강연·해설 성격이 제목에서 함께 확인되는 것을 선별합니다. EBS도 AI와 무관한 교양 영상은 제외합니다. 기업 연구기관의 설명은 독립적인 검증 보도와 다를 수 있습니다. 여행·생활 예능, 쇼츠, 예고, 홍보를 제외합니다. 업로드 날짜는 원방송·촬영 날짜와 다를 수 있습니다.
- 조건에 맞는 콘텐츠가 적으면 개수를 줄입니다. 정상 수집 결과가 0개인 경우도 허용하며 홍보성 콘텐츠로 채우지 않습니다.
- 이는 제목·RSS 메타데이터를 이용하는 편집 규칙입니다. 유명 출처도 오류나 편향이 있을 수 있으며, 원문 전체의 정확성이나 독립적인 교차 검증을 보장하지 않습니다. 추천 라벨은 자동 분류입니다.

기준은 `lib/editorial-policy.js`, 영상 채널은 `lib/video-feeds.js`에서 조정합니다.
